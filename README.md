# KKO.com — โครงสร้างไฟล์

แยกเป็น 3 ไฟล์หลัก เก็บไว้ในโฟลเดอร์เดียวกัน (สำคัญ ห้ามแยกโฟลเดอร์ ไม่งั้นลิงก์ระหว่างไฟล์จะพัง):

```
kko-app/
├── index.html   ← โครงสร้างหน้าเว็บ (login, เมนู, ตัวแปลงไฟล์)
├── style.css    ← ดีไซน์ทั้งหมด
├── script.js    ← โค้ดการทำงานทั้งหมด (ล็อกอิน, เมนู, แปลงไฟล์)
└── README.md    ← ไฟล์นี้
```

## วิธีเปิดใช้งาน
ดับเบิลคลิก `index.html` เปิดในเบราว์เซอร์ได้เลย ไม่ต้องติดตั้งอะไรเพิ่ม ไม่มีการเชื่อมต่อเซิร์ฟเวอร์ภายนอกใดๆ ทั้งสิ้น (ทำงานได้แม้ไม่มีเน็ต) — ถ้าจะเอาขึ้นเว็บจริง อัปโหลดทั้ง 3 ไฟล์ขึ้น hosting ที่ไหนก็ได้ (Netlify, Vercel, GitHub Pages ฯลฯ)

## ทำไมปุ่ม "บัญชีทดลอง" ก่อนหน้านี้ถึงพังจนขึ้น SyntaxError
เวอร์ชันก่อนหน้าโหลดสคริปต์ Google (`accounts.google.com/gsi/client`) จากภายนอก ซึ่งพรีวิวในแชทของ Claude บล็อกโดเมนนี้ไว้ (อนุญาตแค่บาง CDN เท่านั้น) พอสคริปต์โหลดไม่สำเร็จแบบผิดปกติ มันทำให้เกิด error ประเภท "Unexpected end of input" และระบบล็อกอินค้าง

**วิธีแก้ในเวอร์ชันนี้:** เอาการพึ่งพาสคริปต์ภายนอกออกทั้งหมด ปุ่ม "ลงชื่อเข้าใช้ด้วย Google" ตอนนี้ทำงานในเครื่องล้วนๆ (ไม่ยิง request ออกไปไหนเลย) กดแล้วเข้าเมนูได้ทันที ทำงานเสถียร 100% ไม่ว่าเน็ตจะช้าแค่ไหนหรือมือถือสเปคต่ำแค่ไหน

## เกี่ยวกับการล็อกอิน (สำคัญ อ่านตรงนี้)
ตอนนี้ปุ่ม Google เป็น **UI จำลอง** (หน้าตาเหมือนจริง กดแล้วเข้าใช้งานได้ปกติ) ไม่ได้ยืนยันตัวตนกับ Google จริงๆ เพราะการเชื่อม Google OAuth ของจริงต้องมี:
1. Client ID จาก [Google Cloud Console](https://console.cloud.google.com) (สร้างฟรี)
2. โดเมนจริงที่คุณเอาเว็บนี้ไปติดตั้ง (ลงทะเบียนใน "Authorized JavaScript origins")

สิ่งเหล่านี้ใช้งานไม่ได้ในพรีวิวของแชท ต้องรอจนกว่าคุณจะเอาไฟล์นี้ไปโฮสต์บนโดเมนจริงก่อน

**เมื่อพร้อมจะเชื่อมของจริง** ให้ทำตามนี้ใน `index.html` และ `script.js`:

1. เพิ่มใน `<head>` ของ `index.html`:
```html
<script src="https://accounts.google.com/gsi/client?hl=th" async defer></script>
```

2. แทนที่ event listener ของ `googleLoginBtn` ใน `script.js` ด้วย:
```js
google.accounts.id.initialize({
  client_id: "ใส่ CLIENT_ID จริงของคุณที่นี่.apps.googleusercontent.com",
  callback: (response) => {
    const payload = JSON.parse(atob(response.credential.split('.')[1]));
    currentUser = { name: payload.name, picture: payload.picture };
    enterApp();
  }
});
google.accounts.id.renderButton(
  document.getElementById('googleLoginBtn').parentElement,
  { theme: 'filled_black', size: 'large', shape: 'pill', text: 'signin_with' }
);
```

## เกี่ยวกับไฟล์ MP3 ที่ได้
ตัวแปลงไฟล์สกัดเสียงจากวิดีโอได้จริง แต่ไฟล์ที่ได้เป็น **WAV ที่ตั้งชื่อเป็น .mp3** ไม่ใช่ไฟล์ MP3 (MPEG) จริง เล่นได้ปกติในเครื่องเล่นทั่วไป แต่ขนาดไฟล์จะใหญ่กว่า MP3 จริงมาก ถ้าต้องการเข้ารหัส MP3 จริง ต้องเพิ่มไลบรารีเข้ารหัส เช่น `lamejs` — บอกได้เลยถ้าอยากให้เพิ่มให้

## สิ่งที่ปรับเพื่อรองรับมือถือสเปคต่ำ (RAM 4GB ขึ้นไป)
- ไม่มีการโหลดทรัพยากรจากภายนอกเลย (ไม่มี Google Fonts, ไม่มี Google script) → โหลดเร็ว ไม่ค้างรอเน็ต
- ใช้ AudioContext ตัวเดียวซ้ำ แทนสร้างใหม่ทุกครั้งที่แปลงไฟล์ → กันเครื่องค้าง/ชนลิมิตหลังแปลงหลายรอบ
- ลด blur effect ลง และมีพื้นหลังทึบสำรองถ้าเบราว์เซอร์/เครื่องไม่รองรับ backdrop-filter ดีพอ
- ตัด `background-attachment: fixed` ที่ทำให้สกอลล์กระตุกบนมือถือ
- แอนิเมชันคลื่นเสียงตกแต่งจะหยุดนิ่ง ไม่วนตลอดเวลา (ประหยัดแบตเตอรี่/CPU) เหลือแค่ตอนกำลังโหลดจริง
- แจ้งเตือนก่อนถ้าไฟล์วิดีโอใหญ่เกิน ~200MB เพราะขั้นตอนถอดรหัสเสียงเป็นจุดที่กินแรมมากสุด

---

## ตรวจสอบช่องโหว่ความปลอดภัย (Security Review)

ตรวจตามหัวข้อที่แจ้งมา สรุปผลและสิ่งที่แก้ไปแล้ว:

### สิ่งสำคัญที่ต้องเข้าใจก่อน: เว็บนี้ไม่มีเซิร์ฟเวอร์
`index.html` / `style.css` / `script.js` ทำงาน **ทั้งหมดในเบราว์เซอร์ของผู้ใช้เอง** ไม่มี backend ไม่มีฐานข้อมูล ไม่มีที่เก็บไฟล์บนเซิร์ฟเวอร์ ไฟล์ MP4/รูปภาพที่คนเลือก **ไม่เคยถูกส่งออกไปไหนเลย** ประมวลผลอยู่ในแท็บของเขาเอง แล้วก็หายไปเมื่อปิดแท็บ

นี่หมายความว่า: **ถ้ามีคนเลือกไฟล์อันตรายขึ้นมา มันกระทบแค่เครื่อง/แท็บของเขาเอง (ซึ่งเบราว์เซอร์ป้องกันไว้อยู่แล้ว) ไม่มีทาง "ทำลายเว็บไซต์ของคุณ" ได้ เพราะไม่มีอะไรของคุณให้มันไปแตะถึง** ต่างจากเว็บที่มีเซิร์ฟเวอร์รับไฟล์อัปโหลดจริงๆ (ซึ่งเสี่ยงเรื่อง webshell, RCE ฯลฯ) — เว็บแบบ static ล้วนๆ แบบนี้ไม่มีความเสี่ยงกลุ่มนั้นอยู่แล้วโดยโครงสร้าง

### 1. Cookie
ไม่ใช้ cookie เลยทั้งเว็บ (ตรวจสอบแล้ว ไม่มี `document.cookie` ที่ไหนในโค้ด) → ไม่มีอะไรให้ขโมย ไม่มี session cookie ให้ hijack

### 2. Code Logic
- **พบและแก้แล้ว:** ตอน "เลือกรูปปก" โค้ดเดิมเอาชื่อไฟล์ (`file.name`) ไปแปะผ่าน `innerHTML` — ชื่อไฟล์เป็นสิ่งที่ผู้ใช้ควบคุมได้เอง (ตั้งชื่อไฟล์เป็นอะไรก็ได้ รวมถึงใส่โค้ด HTML/JavaScript ในชื่อไฟล์) นี่คือช่องโหว่ **DOM-based XSS** ตัวจริง แก้แล้วโดยเปลี่ยนเป็น `textContent` ซึ่งเบราว์เซอร์จะไม่แปลความเป็น HTML ให้
- **สิ่งที่ควรรู้ (ไม่ใช่บั๊ก แต่เป็นข้อจำกัดโดยธรรมชาติ):** สถานะ "ล็อกอินแล้ว" ตอนนี้เป็นแค่ตัวแปร JavaScript ในเครื่อง ใครเปิด dev tools ก็ข้ามหน้า login ได้เลย — **นี่ปกติสำหรับเว็บที่ไม่มี backend** เพราะไม่มีข้อมูล/ทรัพยากรอะไรให้ป้องกันจริงๆ อยู่หลังบ้านอยู่แล้ว แต่ถ้าวันหนึ่งมีข้อมูลจริงจังที่ต้องป้องกัน (เช่นไฟล์ของผู้ใช้แต่ละคน, ประวัติการใช้งาน) **ห้ามพึ่งการเช็คฝั่ง client เพียงอย่างเดียวเด็ดขาด** ต้องมี backend ที่ตรวจสิทธิ์จริงทุกครั้ง

### 3. Session
ไม่มี server session เพราะไม่มีเซิร์ฟเวอร์ ไม่มี `localStorage`/`sessionStorage` เก็บ token ใดๆ (ตรวจสอบแล้ว) ถ้าอนาคตทำ backend จริง ให้ใช้ cookie แบบ `HttpOnly; Secure; SameSite=Strict` สำหรับ session ไม่ควรเก็บ token ไว้ใน localStorage เพราะถ้ามี XSS หลุดจะขโมย token ไปใช้ได้ทันที

### 4. Metadata
- ไฟล์ที่เลือก (วิดีโอ/รูป) ไม่ถูกอ่าน metadata (EXIF/GPS ฯลฯ) หรือส่งออกไปไหนเลยในโค้ดปัจจุบัน ไม่มีการรั่วไหลของข้อมูลส่วนตัวจากไฟล์
- เพิ่ม security header ผ่าน `<meta>` ใน `index.html`: **Content-Security-Policy** (จำกัดให้โหลดสคริปต์/สไตล์/สื่อได้จากไฟล์ในโดเมนตัวเองเท่านั้น ปิด `<object>`/`<embed>`/`<form>` ทั้งหมด) และ **Referrer-Policy** (ลดข้อมูลที่รั่วไปเว็บอื่นตอนมีลิงก์ออก)
- **ข้อจำกัดของ meta tag:** บาง header อย่าง `X-Frame-Options`, `X-Content-Type-Options`, และ `frame-ancestors` ตั้งผ่าน `<meta>` ไม่ได้จริง ต้องตั้งเป็น HTTP header จริงตอน deploy ดูตัวอย่างด้านล่าง

### 5. ไฟล์อัปโหลด (MP4 / รูปปก)
เพิ่มการป้องกันหลายชั้นใน `script.js`:
- เช็ค `file.type` จริงก่อนประมวลผลทุกครั้ง (attribute `accept` บน `<input>` เป็นแค่ตัวกรองหน้าตา ข้ามได้ง่ายๆ โดยเลือก "ไฟล์ทั้งหมด" หรือลาก-วางไฟล์อื่นเข้ามา) — ถ้าไม่ใช่ประเภทที่ถูกต้อง ปฏิเสธทันทีพร้อมข้อความแจ้งเตือน
- จำกัดขนาดไฟล์: วิดีโอไม่เกิน 500MB (แข็ง), รูปปกไม่เกิน 10MB — กันเครื่องสเปคต่ำแฮงก์/หน่วยความจำเต็มตั้งแต่ตอนเลือกไฟล์ ไม่ต้องรอไปพังตอนแปลง
- ไฟล์วิดีโอ/รูปที่เลือกจะถูกส่งเข้า Web Audio API (`decodeAudioData`) เท่านั้น ซึ่งเป็นฟังก์ชันมาตรฐานของเบราว์เซอร์ที่ทำ sandbox ไว้อยู่แล้ว ไม่มีการ "รัน" ไฟล์เป็นโค้ดในทางใดทางหนึ่ง — ถ้าไฟล์ไม่ใช่สื่อจริงๆ มันแค่ decode ไม่สำเร็จแล้วโชว์ error สุภาพ ไม่มีทางถูกใช้เป็นช่องทางรันโค้ดอันตราย
- ชื่อไฟล์ MP3 ที่ดาวน์โหลด (มาจากช่อง "ชื่อเพลง" ที่ผู้ใช้พิมพ์เอง) ตอนนี้ถูก sanitize ตัดอักขระที่เสี่ยง/ผิดกฎของชื่อไฟล์ออกก่อนเสมอ

### สำหรับตอน deploy จริง (ตั้งค่าที่ hosting)
เพิ่ม HTTP header เหล่านี้ที่ hosting provider (meta tag ตั้งไม่ได้จริง) ตัวอย่างสำหรับ Netlify — สร้างไฟล์ชื่อ `_headers` วางคู่กับไฟล์อื่นๆ:
```
/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Content-Security-Policy: default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self'; media-src 'self' blob:; object-src 'none'; base-uri 'none'; form-action 'none'; frame-ancestors 'none';
```
ถ้าใช้ hosting อื่น (Vercel, GitHub Pages, Cloudflare Pages ฯลฯ) หลักการเดียวกัน แค่รูปแบบไฟล์ config ต่างกัน บอกได้ถ้าอยากให้ช่วยตั้งให้ตาม provider ที่ใช้จริง

### หมายเหตุ: ทดสอบด้วย file:// ในเครื่อง
ถ้าเปิด `index.html` ตรงๆ (ดับเบิลคลิก) แล้วหน้าเว็บดูแปลกๆ/สไตล์ไม่ขึ้น ให้ลองลบบรรทัด `<meta http-equiv="Content-Security-Policy" ...>` ออกชั่วคราว (บางเบราว์เซอร์จัดการ CSP กับไฟล์ local แปลกกว่าไฟล์บนเว็บจริง) หรือใช้เซิร์ฟเวอร์เล็กๆ ทดสอบแทน เช่น `python3 -m http.server` แล้วเปิด `http://localhost:8000`

---

## อัปเดต: เพิ่มแท็บที่ 2 — แปลง รูปภาพ → PDF

### สิ่งที่เพิ่มเข้ามา
- **แถบสลับแท็บ (Tab Bar):** อยู่ในหน้าเดียวกัน สลับระหว่าง 🎵 MP4 to MP3 และ 🖼️ IMG to PDF ได้ทันที ไม่โหลดหน้าใหม่ (แค่ซ่อน/โชว์ panel ด้วย CSS ล้วนๆ)
- **เลือกได้หลายรูปพร้อมกัน:** ช่อง input รองรับ `multiple` เลือกรูปถ่ายเอกสารได้ทีละหลายใบ และเลือกเพิ่มได้เรื่อยๆ (ไม่ได้แทนที่ของเดิม)
- **พรีวิวรูปภาพ (Thumbnails):** โชว์รูปที่เลือกแล้วเป็น grid พร้อมปุ่ม × ลบทีละรูปได้
- **จัดหน้า A4 อัตโนมัติ:** ใช้ jsPDF คำนวณสัดส่วนภาพเทียบกับขนาดหน้า A4 (210×297mm มีขอบ 10mm) ย่อ/ขยายให้พอดีโดยไม่บิดสัดส่วน แล้ววางกึ่งกลางหน้าเสมอ — รูปแนวตั้ง แนวนอน หรือสี่เหลี่ยมจัตุรัส ก็ไม่เบี้ยว ไม่หลุดขอบ
- แต่ละรูป = 1 หน้า PDF เรียงตามลำดับที่เลือก
- ปุ่มดาวน์โหลด, ดูตัวอย่าง PDF (เปิดแท็บใหม่), และแชร์ไฟล์ (รองรับมือถือที่เปิด Web Share API)

### Library ที่ใช้เพิ่ม: jsPDF
โหลดจาก `cdnjs.cloudflare.com` (CDN สาธารณะที่เชื่อถือได้ ใช้กันทั่วไป) — เพิ่มโดเมนนี้ไว้ใน CSP (`script-src`) แล้วโดยเฉพาะ ไม่ได้เปิดกว้างให้โดเมนอื่น

### ข้อจำกัดที่ตั้งใจไว้ (กันเครื่องสเปคต่ำแฮงก์)
- เลือกได้สูงสุด **40 รูปต่อ PDF หนึ่งไฟล์** (แต่ละรูปต้องโหลดเข้าหน่วยความจำเพื่อวาดลง canvas ก่อนใส่ PDF)
- รูปแต่ละใบไม่เกิน **25MB**
- ระบบ flatten รูปที่มีพื้นหลังโปร่งใส (PNG) เป็นพื้นขาวก่อนเสมอ กันปัญหาพิมพ์ออกมาดำ

### ฟังก์ชันแรก (MP4 → MP3) ยังอยู่ครบ
ไม่มีอะไรเปลี่ยนในฟังก์ชันแปลงเสียง แค่ย้ายไปอยู่ใต้แท็บแรกของหน้า workspace เดียวกัน

---

## อัปเดต: เปลี่ยนข้อความท้ายเว็บ
เปลี่ยน footer เป็น:
- "สงวนลิขสิทธิ์สำหรับ SOUKPHANSA SORMIXAY"
- ติดต่อ: 020 54336649 (กดโทรออกได้เลยบนมือถือ)

(ชื่อแบรนด์ "KKO.com" ที่ใช้แสดงในหน้า login/เมนู/title ยังเหมือนเดิม เพราะที่ขอเปลี่ยนคือข้อความท้ายเว็บโดยเฉพาะ — บอกได้ถ้าอยากให้เปลี่ยนชื่อแบรนด์ทั้งเว็บด้วย)

---

## อัปเดตใหญ่: ของตกแต่งมืออาชีพ + หน้าที่จำเป็น

### ไฟล์ใหม่ทั้งหมด
```
kko-app/
├── index.html          ← หน้าแอปหลัก (login, เมนู, แปลงไฟล์)
├── style.css            ← ดีไซน์ทั้งหมด (รวมธีมสว่าง/มืด)
├── script.js             ← โค้ดแอปหลัก (เฉพาะ index.html)
├── theme.js              ← สลับธีม + ลงทะเบียน service worker (ทุกหน้าใช้ร่วมกัน)
├── manifest.json          ← ทำให้ติดตั้งเป็นแอปได้ (PWA)
├── sw.js                   ← service worker สำหรับใช้งานออฟไลน์
├── privacy.html             ← หน้านโยบายความเป็นส่วนตัว
├── about.html                ← หน้าเกี่ยวกับเรา/ติดต่อ
├── icon-192.png, icon-512.png, apple-touch-icon.png, favicon.svg, favicon-32.png
├── og-banner.png              ← รูปตอนแชร์ลิงก์ในไลน์/เฟซบุ๊ก
├── .nojekyll
└── README.md
```
**สำคัญ:** ทุกไฟล์ต้องอยู่โฟลเดอร์เดียวกันเสมอ (อ้างอิงกันด้วย path สัมพัทธ์)

### 1. ลาก-วางไฟล์ (Drag & Drop)
ทุกช่องอัปโหลด (MP4, รูปปก, รูปสำหรับ PDF) ลากไฟล์มาวางได้เลย ไม่ต้องกดเลือกอย่างเดียว มีไฮไลต์สีเขียวตอนลากไฟล์ผ่านเข้ามา ใช้โค้ดชุดเดียว (`wireDropZone`) กับทุกช่อง ไม่ซ้ำโค้ด

### 2. แถบ Progress % ของจริง
- **MP4→MP3:** อ่านไฟล์ (0-55%) ใช้ progress จริงจาก `FileReader` ตามไบต์ที่อ่านจริง ส่วนขั้นถอดรหัสเสียงเป็นแถบวิ่งไม่หยุด (indeterminate) เพราะ Web Audio API ไม่มี progress event ให้ — ไม่ปั้นตัวเลขปลอมๆ ให้ดูสวยแต่ไม่จริง
- **รูปภาพ→PDF:** progress จริง 100% เพราะแปลงทีละรูปตามลำดับ รู้ % ที่แน่นอนทุกขั้น (`กำลังจัดหน้า 3/8` แบบนี้)

### 3. PWA (ติดตั้งเป็นแอป + ใช้ออฟไลน์ได้)
- มี `manifest.json` + ไอคอนแอปครบ (192px, 512px, apple-touch-icon)
- มี `sw.js` (service worker) แคชไฟล์แอปไว้ใช้ออฟไลน์ รวมถึง jsPDF ด้วย
- เปิดเว็บใน Chrome/Edge บนมือถือจะเห็นตัวเลือก "ติดตั้งแอป" หรือ "เพิ่มไปหน้าจอหลัก" อัตโนมัติ (Safari iOS ใช้ปุ่มแชร์ → "เพิ่มไปยังหน้าจอโฮม")
- **ข้อจำกัดสำคัญ:** Service Worker ทำงานได้เฉพาะบน HTTPS หรือ localhost เท่านั้น (กฎของเบราว์เซอร์ ไม่ใช่ข้อจำกัดจากโค้ดเรา) — เปิดผ่าน `file://` ตรงๆ จะไม่ติดตั้ง/ออฟไลน์ได้ ต้อง deploy ขึ้นเว็บจริง (GitHub Pages ใช้ HTTPS อยู่แล้ว ใช้งานได้เต็มรูปแบบ)
- ทุกครั้งที่แก้ไฟล์แล้ว deploy ใหม่ ให้เปลี่ยนเลขใน `CACHE_NAME` ที่ต้นไฟล์ `sw.js` (เช่น `kko-app-v1` → `kko-app-v2`) ไม่งั้นผู้ใช้เก่าจะยังเห็นไฟล์แคชเวอร์ชันเดิมอยู่

### 4. สลับธีมสว่าง/มืด
ปุ่มวงกลม ☀️/🌙 มุมขวาบน อยู่ทุกหน้า (index, privacy, about) จำค่าไว้ใน `localStorage` เปิดครั้งต่อไปยังเป็นธีมเดิม ถ้ายังไม่เคยเลือก จะตามการตั้งค่าธีมของเครื่อง (`prefers-color-scheme`) ให้อัตโนมัติ

### 5. ประวัติการใช้งานล่าสุด
โชว์ในหน้าเมนู ใต้แทร็ก 01/02 เก็บแค่ "ชื่อไฟล์ + เวลา" ผ่าน `localStorage` (ไม่ใช่ตัวไฟล์จริง ไม่ส่งไปไหน) เก็บสูงสุด 10 รายการล่าสุด มีปุ่ม "ล้างประวัติ" (มี dialog ยืนยันก่อนลบ)

### 6. Toast + Confirm Dialog แบบกำหนดเอง
เลิกใช้ `alert()`/`confirm()` ของเบราว์เซอร์ทั้งหมดแล้ว แทนที่ด้วย:
- **Toast** — ข้อความแจ้งเตือนมุมล่าง หายไปเองใน ~3 วิ (สีแดงถ้าเป็น error)
- **Confirm modal** — กล่องยืนยันกลางจอ ใช้ตอนเตือนไฟล์ใหญ่ และตอนล้างประวัติ

### 7. ฟอนต์ใหม่ (ดูเป็นลายมือขึ้น)
เปลี่ยนฟอนต์หัวข้อ/ปุ่ม/แบรนด์เป็น **Mali** (ฟอนต์ไทยแนวลายมือ อ่านง่าย ให้ความรู้สึกเป็นกันเองกว่า Kanit เดิม) โหลดจาก Google Fonts คู่กับ **Sarabun** (ใช้กับข้อความยาวๆ อ่านสบายตากว่า) ถ้าเน็ตช้า/บล็อก จะ fallback กลับไปฟอนต์ระบบอัตโนมัติ ไม่พังหน้าเว็บ

### 8. หน้านโยบายความเป็นส่วนตัว + เกี่ยวกับเรา
`privacy.html` และ `about.html` — ลิงก์อยู่ในฟุตเตอร์ทุกหน้า อธิบายจุดขาย "ไฟล์ไม่ออกจากเครื่องคุณ" ให้ชัดเจน สร้างความน่าเชื่อถือ

### 9. Favicon + Social Share
- Favicon (SVG + PNG fallback), apple-touch-icon สำหรับ iOS
- Open Graph + Twitter Card meta tags — แปะลิงก์เว็บนี้ในไลน์/เฟซบุ๊ก/ทวิตเตอร์ จะโชว์รูปปก `og-banner.png` พร้อมชื่อ/คำอธิบายสวยๆ

**ก่อน deploy จริง อย่าลืม:** เปิด `index.html`, `privacy.html`, `about.html` แล้วแก้ `<meta property="og:image" content="og-banner.png">` ให้เป็น URL เต็ม เช่น `https://yourdomain.com/og-banner.png` (บาง platform ต้องการ URL แบบเต็ม ไม่ใช่ path สัมพัทธ์ ถึงจะแสดงรูปตอนแชร์ลิงก์ได้)

### หมายเหตุเรื่อง localStorage
ใช้เก็บแค่ 2 อย่าง: ธีมที่เลือก และประวัติการใช้งาน (ชื่อไฟล์+เวลา) — ไม่มีข้อมูลอ่อนไหวใดๆ เก็บไว้ ปลอดภัยตามหลักที่คุยกันไว้ก่อนหน้านี้เรื่อง session/token

---

## อัปเดต: เพิ่ม 5 แทร็กใหม่ (รวมเป็น 7 แทร็ก)

เพิ่มเฉพาะกลุ่ม "ทำง่าย เบา เหมาะกับสเปคต่ำ" ตามที่คุยกันไว้ ส่วนกลุ่มหนัก (OCR, ตัดต่อวิดีโอ, ลบพื้นหลังอัตโนมัติ) ยังไม่ทำ เพราะจะกระทบเครื่องสเปคต่ำตามที่เตือนไว้ก่อนหน้านี้ — บอกได้ถ้าอยากให้ทำเพิ่มถึงจะรู้ข้อจำกัดแล้ว

แถบแท็บตอนนี้มี 7 อัน เลื่อนซ้าย-ขวาได้ (scrollable) เพราะยัดในแถวเดียวไม่พอ

### แทร็ก 03 — 🔳 สร้าง QR Code
พิมพ์ข้อความ/ลิงก์ → กดสร้าง → ดาวน์โหลดเป็น PNG หรือแชร์ได้เลย ใช้ไลบรารี `qrcodejs` (เบามาก ไม่ถึง 15KB) กรอบพื้นหลังเป็นสีขาวเสมอไม่ว่าจะใช้ธีมไหน เพราะ QR ต้องคอนทราสต์ขาวดำถึงจะสแกนได้ชัวร์

### แทร็ก 04 — 🗜️ บีบอัดรูปภาพ
เลือกรูป → ปรับสไลเดอร์คุณภาพ (10-100%) → เห็นขนาดไฟล์ก่อน-หลังบีบอัดชัดเจน ใช้ canvas ล้วนๆ ไม่พึ่ง library ภายนอก

### แทร็ก 05 — 🔄 แปลงฟอร์แมตรูป
สลับ PNG / JPG / WebP ได้ในคลิกเดียว (ถ้าเบราว์เซอร์ไม่รองรับ WebP จะแจ้งเตือนแทนที่จะพัง) ใช้เทคนิค canvas เดียวกับตัวบีบอัด

### แทร็ก 06 — 📎 รวมไฟล์ PDF
เลือกหลายไฟล์ PDF พร้อมกัน โชว์รายการ+ขนาดไฟล์ ลบทีละไฟล์ได้ก่อนรวม รวมตามลำดับที่เลือก ใช้ไลบรารี `pdf-lib` (คัดลอกหน้าทุกหน้าจากทุกไฟล์มาต่อกันเป็นไฟล์เดียว ไม่เข้ารหัสซ้ำ เร็วและคุณภาพเท่าต้นฉบับ) ต้องเลือกอย่างน้อย 2 ไฟล์

### แทร็ก 07 — 🖼️ ใส่ลายน้ำลงรูป
เลือกรูป → พิมพ์ข้อความ เลือกตำแหน่ง (5 มุม) ปรับความโปร่งใส → เห็นพรีวิวสดทันทีบน canvas ก่อนดาวน์โหลด กดดาวน์โหลดได้เลยไม่ต้องรอประมวลผล (เร็วเพราะวาดรูปเดียว)

### ของแถม: หมุนรูปในแท็บ IMG → PDF
เพิ่มปุ่ม ↻ ที่มุมล่างซ้ายของแต่ละรูปในแท็บ 02 กดหมุนทีละ 90° ระบบจะจำมุมหมุนของแต่ละรูปแยกกัน แล้วใช้ตอนสร้าง PDF จริง (ไม่ใช่แค่หมุนพรีวิว)

**หมายเหตุ:** ตั้งใจไม่ทำ "ครอปรูป" เพราะต้องมี UI ลากขอบ/พื้นที่ตัด ซึ่งซับซ้อนกว่าอย่างอื่นในกลุ่มนี้มาก (ต่างจากหมุนที่กดปุ่มเดียวจบ) ถ้าอยากได้จริงๆ บอกได้ จะทำเป็นงานแยกให้

### Library ใหม่ที่เพิ่ม (โหลดจาก cdnjs เหมือนเดิม)
- `qrcodejs` — สร้าง QR Code
- `pdf-lib` — รวมไฟล์ PDF
เพิ่มทั้งสองใน CSP แล้ว (โดเมน cdnjs.cloudflare.com อนุญาตไว้อยู่แล้วจากตอนเพิ่ม jsPDF ไม่ต้องแก้อะไรเพิ่ม) และเพิ่มเข้า `sw.js` ให้แคชไว้ใช้ออฟไลน์ด้วย — **อัปเดต `CACHE_NAME` เป็น `kko-app-v2` แล้ว** เพื่อให้ผู้ใช้เดิมได้ไฟล์เวอร์ชันใหม่แทนของแคชเก่า

### ประวัติการใช้งาน
รองรับไอคอนของทุกเครื่องมือแล้ว: 🎵 MP3, 📄 PDF, 🔳 QR, 🗜️ บีบอัด, 🔄 แปลงฟอร์แมต, 📎 รวม PDF, 🖼️ ลายน้ำ

---

## อัปเดต: เพิ่มลูกเล่นให้เว็บดูมีชีวิตขึ้น (ตามหัวข้อที่คุยกันไว้ทั้ง 7 ข้อ)

### 1. จัดหมวดหมู่เมนู
7 แทร็กแบ่งเป็น 3 หมวดแล้ว: 🎵 เสียง (แทร็ก 01) · 📄 เอกสาร (แทร็ก 02, 06) · 🖼️ รูปภาพ (แทร็ก 03, 04, 05, 07) — เพิ่มเครื่องมือใหม่ในอนาคตแค่ใส่การ์ดเพิ่มในหมวดที่เกี่ยวข้อง ไม่ต้องคิดใหม่

### 2. ป้าย "ใหม่!"
แทร็ก 03-07 มีป้ายสีทองมุมบนขวาการ์ดแล้ว (ไฟล์ HTML มีคอมเมนต์กำกับไว้ ถ้าวันหนึ่งอยากลบป้ายออกเพราะไม่ใหม่แล้ว ลบ `<span class="new-badge">ใหม่!</span>` ออกจากการ์ดนั้นได้เลย)

### 3. ปุ่มแชร์แอป
ปุ่มบนสุดของหน้าเมนู ใช้ Web Share API ถ้าเบราว์เซอร์รองรับ (ส่วนใหญ่มือถือรองรับ) ถ้าไม่รองรับจะคัดลอกลิงก์ให้แทนอัตโนมัติ

### 4. สถิติการใช้งาน
เก็บตัวนับแยกจากประวัติ (ประวัติเก็บแค่ 10 รายการล่าสุด แต่ตัวนับนี้นับสะสมไปเรื่อยๆ ไม่มีเพดาน) โชว์เป็น "ใช้งานไปแล้ว X ครั้ง 🎉" ใต้คำทักทาย

### 5. คำทักทายตามเวลาจริง
เปลี่ยนอัตโนมัติ: เช้า/บ่าย/เย็น/ดึก ตามเวลาเครื่องผู้ใช้ตอนเปิดหน้าเมนู

### 6. คอนเฟตตี้ฉลองความสำเร็จ
ทำงานครบทั้ง 7 เครื่องมือ (แปลง MP3, PDF, QR, บีบอัด, แปลงฟอร์แมต, รวม PDF, ลายน้ำ) เบามาก ใช้แค่ CSS animation ไม่มี library เพิ่ม และเคารพการตั้งค่า "ลดการเคลื่อนไหว" (prefers-reduced-motion) ของเครื่องผู้ใช้อัตโนมัติ — ถ้าเครื่องไหนตั้งค่านั้นไว้ คอนเฟตตี้จะไม่เด้งเลย

### 7. หน้า Landing ก่อนเข้าสู่ระบบ
เพิ่มหน้าแรกใหม่ (ก่อนหน้า login) แนะนำว่าเว็บนี้ทำอะไรได้บ้าง มีปุ่ม "เริ่มใช้งานฟรี" ถึงจะไปหน้าล็อกอิน — คนที่ไม่รู้จักเว็บมาก่อนจะเห็นภาพรวมก่อนตัดสินใจกดเข้าใช้

## อัปเดต: อีเมลติดต่องาน
เพิ่ม **koko881681@gmail.com** ในฟุตเตอร์ทุกหน้า (index, privacy, about) และในหน้า "เกี่ยวกับเรา"/"ความเป็นส่วนตัว" ด้วย แยกจากเบอร์โทรที่มีอยู่แล้ว ใช้สำหรับติดต่องานและข้อมูลตามที่แจ้ง

## Service Worker
อัปเดตเป็น `kko-app-v3` แล้ว (จาก v2) เพราะไฟล์หลักเปลี่ยนเยอะรอบนี้ — ผู้ใช้เดิมที่เคยติดตั้งแอปไว้จะได้เวอร์ชันใหม่อัตโนมัติในการเปิดครั้งถัดไป

---

## อัปเดต: เพิ่มแทร็ก 08 — แปลภาษา (ไม่ต้องมี backend)

### ทำไมไม่ต้องมี backend
GitHub Pages รันเซิร์ฟเวอร์ (Python/Node.js) ไม่ได้เลย — เป็นข้อจำกัดถาวรของแพลตฟอร์ม เครื่องมือแปลภาษานี้เลยใช้ **MyMemory Translation API** แทน Google Translate เพราะ MyMemory ไม่ต้องมีกุญแจ API ลับ เรียกจาก JavaScript ในเบราว์เซอร์ตรงๆ ได้อย่างปลอดภัย (ไม่มีความลับให้รั่วไหลตั้งแต่แรก) ทำงานบน GitHub Pages ได้ทันทีเหมือนอีก 7 เครื่องมือ

### ข้อจำกัดของ MyMemory (ฟรี)
- **5,000 ตัวอักษร/วัน ต่อ IP** — เพราะเรียกจากเบราว์เซอร์ผู้ใช้แต่ละคนโดยตรง แต่ละคนที่เข้าเว็บจะมีโควตาแยกของตัวเอง (ข้อดีของการไม่มี backend กลาง: โควตาไม่ใช้ร่วมกันทั้งเว็บ ใครสแปมก็กระทบแค่โควตาของตัวเอง)
- จำกัดข้อความไม่เกิน **480 ตัวอักษรต่อครั้ง** (ตัว API จำกัดจริงๆ อยู่ที่ ~500 ไบต์ ข้อความภาษาไทย/หลายไบต์กินพื้นที่มากกว่าอังกฤษ เลยตั้งเผื่อไว้)
- **ไม่รองรับ "ตรวจจับภาษาอัตโนมัติ"** อย่างเป็นทางการ (ทดสอบแล้วพบเคสที่ API ปฏิเสธ auto-detect ตรงๆ) จึงตั้งใจไม่ใส่ตัวเลือกนี้ ให้เลือกภาษาต้นทาง-ปลายทางชัดเจนแทน เพื่อไม่ให้ฟีเจอร์พังแบบไม่รู้ตัว
- คุณภาพแปล: ใช้ได้ดีสำหรับข้อความทั่วไป/ประโยคสั้น แต่ไม่เนียนเท่า Google Translate เพราะผสมข้อมูลแปลจากคนจริงกับเครื่อง

### ถ้าอยากได้คุณภาพระดับ Google Translate ในอนาคต
ต้องทำเพิ่ม 2 อย่าง: (1) สมัคร Google Cloud Translation API ผูกบัตรเครดิต (ฟรี 500,000 ตัวอักษร/เดือนแรก) (2) เอา backend ไปฝากไว้ที่อื่นที่รันโค้ดได้ เช่น Cloudflare Workers (free tier ดี) แล้วให้เว็บนี้ยิง fetch ไปหา backend นั้นแทนที่จะยิงตรงไป Google — บอกได้ถ้าอยากให้ช่วยวางระบบตอนพร้อม

### ความปลอดภัย
- ผลลัพธ์การแปลแสดงผ่าน `textContent` เท่านั้น (กัน XSS จากข้อความที่พิมพ์เข้ามาหรือได้จาก API)
- เพิ่มเฉพาะโดเมน `api.mymemory.translated.net` เข้า CSP (`connect-src`) แบบเจาะจง ไม่เปิดกว้าง
- ไม่มีกุญแจ/ความลับใดๆ ในโค้ด — ต่อให้เปิดดู source code ก็ไม่มีอะไรให้ขโมยไปใช้แทนได้

Service Worker อัปเดตเป็น `kko-app-v4` แล้ว

---

## อัปเดต: แทร็ก 09 (อ่านหนังสือ/เอกสาร) + แทร็ก 10 (เครื่องคิดเลข) + วิดเจ็ตหน้าเมนู

เพิ่มหมวดใหม่ **📚 นักเรียน** — เป็นก้าวแรกของการวางตัวเว็บให้ชัดว่า "เพื่อนคู่ใจนักเรียน-นักศึกษา" ตามที่คุยกันไว้

### แทร็ก 09 — 📚 อ่านหนังสือ/เอกสาร
ใช้ **pdf.js** (library ของ Mozilla เอง ตัวเดียวกับที่ Firefox/Chrome ใช้เปิด PDF ในตัว) โหลดจาก cdnjs เหมือนของเดิม
- เปิดได้ทั้งไฟล์ **PDF** และ **TXT**
- **จำหน้า/ตำแหน่งที่อ่านค้างไว้** — เก็บในเครื่อง (localStorage) คีย์ตามชื่อไฟล์+ขนาดไฟล์ เปิดไฟล์เดิมอีกครั้งจะเด้งไปหน้า/ตำแหน่งเดิมให้อัตโนมัติ (PDF จำเป็น "หน้า", TXT จำเป็น "% ตำแหน่งสกอลล์")
- **ปรับขนาดตัวอักษร** — ปุ่ม A−/A+ (PDF จะซูมทั้งหน้า, TXT จะปรับ font-size จริงๆ)
- **อ่านออกเสียง** — ใช้ Web Speech API ในตัวเบราว์เซอร์ (ฟรี ไม่มี library เพิ่ม) ดึงข้อความจากหน้า PDF ที่เปิดอยู่ หรือทั้งไฟล์ถ้าเป็น TXT ตัดข้อความเป็นท่อนสั้นๆ ก่อนส่งเข้าระบบเสียงเพื่อความเสถียร (ข้อความยาวมากๆ ในคราวเดียวมักมีปัญหากับ Web Speech API)
- **แถบ progress** แสดง % อ่านไปแล้วเสมอ

**ข้อจำกัดที่ควรรู้:**
- คุณภาพเสียงอ่าน ขึ้นอยู่กับเสียงที่เครื่อง/เบราว์เซอร์ของผู้ใช้แต่ละคนมีติดตั้งไว้ — เสียงไทยบนบางอุปกรณ์อาจไม่มีให้เลือก (ข้อจำกัดของแพลตฟอร์ม ไม่ใช่โค้ดเรา)
- จำกัดไฟล์ไม่เกิน 80MB และอ่านออกเสียงได้ครั้งละไม่เกิน ~20,000 ตัวอักษร (กันเครื่องสเปคต่ำค้าง)
- ไม่รองรับไฟล์ EPUB (มาตรฐานอีบุ๊กจริงจัง) เพราะต้องใช้ library เพิ่มอีกตัวและซับซ้อนกว่านี้มาก — เริ่มจาก PDF/TXT ก่อนซึ่งครอบคลุมการใช้งานส่วนใหญ่ของนักเรียน (เอกสารจากอาจารย์ ชีทสรุป ก็เป็น PDF อยู่แล้ว)

### แทร็ก 10 — 🧮 เครื่องคิดเลข
เครื่องคิดเลข 4 ฟังก์ชันพื้นฐาน (บวก ลบ คูณ หาร, +/-, %, AC) ปุ่มพฤติกรรมแบบเดียวกับเครื่องคิดเลขมือถือทั่วไป ไม่มี dependency ภายนอกเลย เบาที่สุดในบรรดา 10 เครื่องมือ (ตั้งใจไม่ก็อปสีส้ม/ดำของ iOS ตรงๆ ใช้โทนสีเดียวกับแบรนด์เราแทน เพื่อความเป็นเอกลักษณ์)

### วิดเจ็ตหน้าเมนู
- **⏳ นับถอยหลังวันสอบ/วันส่งงาน** — ตั้งชื่อ+วันที่ได้เอง เก็บในเครื่อง กด "แก้ไข" เปลี่ยนได้ตลอด
- **💬 คำคมให้กำลังใจ** — สุ่มจากคลัง 10 ประโยค เปลี่ยนทุกครั้งที่เข้าเมนู

## Service Worker
อัปเดตเป็น `kko-app-v5` แล้ว (เพิ่ม pdf.js เข้ารายการแคชสำหรับใช้ออฟไลน์ด้วย)

## แนวคิดต่อยอด (ยังไม่ได้ทำ บอกได้ถ้าอยากให้เพิ่ม)
- 📐 ตัวช่วยจัดรูปแบบอ้างอิง APA/MLA
- 🧮 คำนวณเกรดเฉลี่ย (GPA)
- ⏰ Pomodoro / ตัวจับเวลาอ่านหนังสือ

---

## อัปเดต: รีดีไซน์ทั้งเว็บ — ธีม "ฤดูฝน" (Rainy Mood)

เปลี่ยนภาษาการออกแบบทั้งหมดจากโทนเขียวนีออน/ดำล้วน (ที่ดู "AI ทั่วไป") เป็นธีมใหม่ตามสเปกที่ส่งมา **ไม่ได้แตะ JavaScript logic ของเครื่องมือทั้ง 10 อันเลย** งานนี้เป็นงาน CSS/มาร์กอัปล้วนๆ ฟังก์ชันทำงานเหมือนเดิมทุกอย่าง

### สีและบรรยากาศ
- พื้นหลังเปลี่ยนจากดำล้วนเป็น **สีกรมท่าเข้มอมเทา** (`#1a2332`) ให้ความรู้สึกฟ้าครึ้มฝนตก
- สีเน้น (accent) เปลี่ยนจากเขียวนีออนเป็น **สีฟ้าหยดน้ำ** (`#6ec3ff`)
- ธีมสว่างก็ปรับให้เข้าธีมเดียวกัน (โทนฟ้าเทาอ่อน แทนเขียวอ่อนเดิม)
- ไอคอนแอป/favicon/รูปแชร์ลิงก์ (OG banner) รีเจนใหม่ให้เข้าธีมฟ้าทั้งหมดแล้ว

### Glassmorphism
เพิ่ม `backdrop-filter: blur()` ให้การ์ด, แถบแท็บ, การ์ดเครื่องมือ, วิดเจ็ต, user chip, รายการประวัติ — ให้ความรู้สึกกระจกฝ้าโปร่งแสงจริงจังขึ้น (ยังมี fallback พื้นทึบสำหรับเบราว์เซอร์ที่ไม่รองรับเหมือนเดิม ไม่กระทบเครื่องสเปคต่ำ)

### ฟอนต์
เปลี่ยนจาก Mali/Sarabun เป็น **Prompt** ตัวเดียวทั้งเว็บ (ตามสเปก) — ฟอนต์ไทยทันสมัย กะทัดรัด อ่านง่ายบนจอมือถือ

### โลโก้ตัวอักษร (Typography Logo)
เปลี่ยนจากตัวอักษรธรรมดาเป็นโลโก้ที่มีเอฟเฟกต์แสงส่องวิ่งผ่านเบาๆ (CSS gradient sheen animation) พร้อม**หยดน้ำสีฟ้าเล็กๆ ลอยข้างโลโก้** (SVG กำหนดเอง มีแอนิเมชันลอยขึ้นลงนุ่มๆ) ใช้ทุกจุดที่เป็นโลโก้หลัก: หน้า Landing, หน้า Login, แถบบนของเมนู, และหน้าเกี่ยวกับเรา/ความเป็นส่วนตัว — เคารพการตั้งค่า "ลดการเคลื่อนไหว" ของเครื่องผู้ใช้เหมือนจุดอื่นๆ ในเว็บ

### แท็กหมวดหมู่ (Color-coded Tags)
หมวดหมู่บนหน้าคลังเครื่องมือมีสีพาสเทลแยกชัดเจนแล้ว:
- 🎵 เสียง — ส้มพีช
- 📄 เอกสาร — เหลืองอำพัน
- 🖼️ รูปภาพ — ม่วงลาเวนเดอร์
- 🌐 ภาษา — ฟ้าอ่อน/ข้อความฟ้าเข้ม (ตามสเปกที่ระบุมา)
- 📚 นักเรียน — เขียวมิ้นท์

### พื้นที่อัปโหลดไฟล์ (Borderless)
เอาพื้นหลังทึบออกจากทุกช่องอัปโหลด (ทั้ง 6 เครื่องมือที่มีการอัปโหลดไฟล์) เหลือแค่เส้นประบางโปร่งแสง พื้นหลังโปร่งใสจนกว่าจะแตะ/ลากไฟล์เข้ามา ถึงจะติดสีฟ้าอ่อนนุ่มๆ ให้ความรู้สึกโล่ง ไม่อึดอัดแบบกล่องทึบเดิม

## ไฟล์ที่แก้/รีเจนในรอบนี้
- `style.css` — เปลี่ยน palette, ฟอนต์, glass effect, โลโก้, แท็กสี, upload area ทั้งหมด
- `index.html`, `privacy.html`, `about.html` — เปลี่ยนฟอนต์ลิงก์, theme-color, มาร์กอัปโลโก้ใหม่, class สีแท็กหมวดหมู่
- `script.js` — แก้แค่สีคอนเฟตตี้ให้เข้าธีมใหม่ (บรรทัดเดียว ไม่กระทบ logic)
- `manifest.json` — อัปเดต theme_color/background_color
- ไอคอนแอปทุกขนาด + favicon + og-banner.png — รีเจนใหม่เป็นโทนฟ้า
